# Redirects — Cloudflare Pages (spec técnica §4/§45).
# Domínio canônico definido em 2026-08-26 (auditoria SEO): f5sg.com.br.

# HTTP → HTTPS é resolvido pelo próprio Cloudflare Pages (SSL/TLS "Always Use
# HTTPS") — não duplicar aqui.

# www → non-www (non-www é o canônico usado em canonical/OG/schema/sitemap):
# `_redirects` do Cloudflare Pages só aceita paths relativos — uma regra de
# host pra host (www.f5sg.com.br → f5sg.com.br) tentada aqui FALHA o build
# ("Only relative URLs are allowed", confirmado rodando `astro build` na
# auditoria SEO de 2026-08-26). Configurar em Cloudflare → domínio →
# Regras de redirecionamento (Redirect Rules) ou Bulk Redirects, no nível de
# zona/DNS, não neste arquivo.

# Slugs antigos de anúncios sem o prefixo /siofi, caso existam campanhas ativas
/oficina           /siofi/oficina-mecanica   301
/auto-center       /siofi/auto-center        301

# Trailing slash → sem trailing slash (spec técnica §4)
/siofi/            /siofi                    301
